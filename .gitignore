node_modules/
dist/

# Local env files hold API keys — never commit (owner pastes keys in-session)
.env
.env.local
.env.*.local

dist-landing/

landing-en/bundle/
landing-zh/bundle/

# Supabase CLI local state (project ref/org ids — linked per machine)
supabase/.temp/

