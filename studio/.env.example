# Frontend (static export) — public values only, safe to ship in the JS bundle.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Everything else (Gemini, Google, LINE credentials) is configured as
# Supabase Edge Function secrets, NOT here — see README.md "Getting started"
# step 3, and docs/API.md. Never add secret keys to this frontend project.
